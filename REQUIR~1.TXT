python-telegram-bot>=21.0
# Starlette + uvicorn own the HTTP server so the Telegram webhook and the
# private /tasks/run endpoint can share one port. PTB runs with updater=None
# and is fed through its update queue, so its [webhooks] extra is NOT needed.
starlette>=0.37
uvicorn>=0.30
asyncpg>=0.29
httpx>=0.27
python-dotenv>=1.0.0
